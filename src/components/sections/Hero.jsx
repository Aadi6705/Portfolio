// src/components/sections/Hero.jsx — Phase 0 shell
export default function Hero() {
  return (
    <section id="hero" aria-labelledby="hero-heading" className="section-padding bg-paper">
      <div className="content-container">
        <h1 id="hero-heading" className="font-serif text-display text-ink">
          {/* Phase 2: Typed.js, photo, CTAs */}
          Hero — Phase 2
        </h1>
      </div>
    </section>
  )
}
